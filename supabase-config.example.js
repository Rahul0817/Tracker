// Optional: enables cloud sync.
// 1. Copy this file to `supabase-config.js` (it is gitignored).
// 2. Fill in your Supabase project's URL and anon (public) key
//    from the dashboard: Settings → API.
// Without this file the app runs fully local, exactly as before.
window.HABIT_SUPABASE = {
  url: 'https://YOUR-PROJECT-REF.supabase.co',
  anonKey: 'YOUR-ANON-PUBLIC-KEY',
};
